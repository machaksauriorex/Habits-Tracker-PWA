/**
 * Función pura de la hucha. Recibe el historial completo y devuelve
 * { saldo, rachas, movimientos }. Se implementa en la Fase 2.
 *
 * No usa estado mutable: editar un registro pasado basta con llamar
 * a esta función de nuevo para recalcular todo desde cero.
 */
export function calcularHucha({ habitos, fases, registros, ajustes }) {
  // TODO: Fase 2
  return { saldo: 0, rachas: {}, movimientos: [] }
}
